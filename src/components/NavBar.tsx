import { motion } from 'framer-motion'

export default function NavBar (): JSX.Element {
  return (
    <motion.div className="bg-body-secondary ">
      <div className="container-fluid"></div>
    </motion.div>
  )
}
